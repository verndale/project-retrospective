"use strict";

// @verndale/ai-commit publishes its shared preset at the package root. Keeping the
// public import here gives hooks, local scripts, and CI one authoritative config.
module.exports = require("@verndale/ai-commit");
